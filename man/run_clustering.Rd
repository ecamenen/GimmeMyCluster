% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fingerprint_clustering.R
\name{run_clustering}
\alias{run_clustering}
\title{Perform clustering}
\usage{
run_clustering(
  dist = NULL,
  data = NULL,
  method = 3,
  max_cluster = 6,
  centers = NULL,
  nstart = 100,
  ...
)
}
\arguments{
\item{dist}{A \code{dist} object.}

\item{data}{A matrix or data frame of integers with rows as samples and columns as variables.}

\item{method}{Integer for the clustering method.}

\item{max_cluster}{Integer for the maximum number of clusters.}

\item{centers}{either the number of clusters, say \eqn{k}, or a set of
    initial (distinct) cluster centres.  If a number, a random set of
    (distinct) rows in \code{x} is chosen as the initial centres.}

\item{nstart}{if \code{centers} is a number, how many random sets
    should be chosen?}

\item{...}{Additional arguments for \code{hclust}, \code{kmeans} or \code{pam}.}
}
\value{
A \code{clustering} object for different numbers of clusters.
}
\description{
Perform either hierarchical or non-hierarchical clustering.
}
\examples{
dist_matrix <- dist(iris[, -5])
run_clustering(dist_matrix)
run_clustering(data = iris[, -5], method = 2, max_cluster = 4)
}
