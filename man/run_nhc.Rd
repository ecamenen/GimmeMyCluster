% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fingerprint_clustering.R
\name{run_nhc}
\alias{run_nhc}
\title{Non-Hierarchical Clustering}
\usage{
run_nhc(
  data = NULL,
  dist = NULL,
  n_cluster = 2,
  method = 1,
  centers = NULL,
  nstart = 100,
  ...
)
}
\arguments{
\item{data}{A matrix or data frame of integers.}

\item{dist}{A `dist object.}

\item{n_cluster}{Integer for the number of clusters.}

\item{method}{Integer for the clustering method.}

\item{centers}{either the number of clusters, say \eqn{k}, or a set of
    initial (distinct) cluster centres.  If a number, a random set of
    (distinct) rows in \code{x} is chosen as the initial centres.}

\item{nstart}{if \code{centers} is a number, how many random sets
    should be chosen?}

\item{...}{not used.}
}
\value{
A \code{pam} or \code{kmeans} object.
}
\description{
Perform non-hierarchical clustering using K-means or K-medoids (PAM) methods.
}
\examples{
dist_matrix <- dist(iris[, -5])
run_nhc(dist = dist_matrix)
run_nhc(data = iris[, -5], n_cluster = 3, method = 2)
}
